#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
	ifstream myfile("tinypix.ppm");
	if (myfile.is_open())
	{
		int i;
		while (i < i - 1, i++)
		{
			getline(read,i);
		}
	}
	return 0;
}
